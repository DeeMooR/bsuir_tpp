<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/fragmentDelete"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/white"
    android:textColor="@color/black"
    android:paddingTop="65dp"
    android:paddingHorizontal="16dp"
    tools:context=".MainActivity">

    <TextView
        android:id="@+id/titleFragDelete"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="DELTE NOTE"
        android:gravity="center"
        android:layout_marginBottom="20dp"
        android:textSize="22dp"/>

    <TextView
        android:id="@+id/textFragDelete"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Write note id:"
        android:textSize="18dp"/>
    <EditText
        android:id="@+id/inputIdFragDelete"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:inputType="text"
        android:textSize="18dp"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center_horizontal"
        android:layout_marginTop="30dp">

        <Button
            android:id="@+id/btnDeleteOne"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:backgroundTint="@color/red"
            android:paddingHorizontal="30dp"
            android:paddingVertical="5dp"
            android:layout_marginBottom="15dp"
            android:text="DELETE ONE"
            android:textSize="16dp" />

        <Button
            android:id="@+id/btnDeleteAll"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:backgroundTint="@color/red"
            android:paddingHorizontal="30dp"
            android:paddingVertical="5dp"
            android:text="DELETE ALL"
            android:textSize="16dp" />
    </LinearLayout>
</LinearLayout>